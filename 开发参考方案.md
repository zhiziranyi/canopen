# CANopen CiA 402 FOC 伺服驱动系统 — 参考方案

## 项目简介

基于 CANopen 协议的 FOC 伺服驱动器，STM32F407ZGT6（鹿小班版），2804 无刷电机，AS5600 磁编码器。当前硬件未安装 INA240，固件使用无电流传感器的电压型 FOC。

---

## 硬件接线

### STM32F407 → SimpleFOC Mini

| STM32 | SimpleFOC Mini | 说明 |
|-------|---------------|------|
| **PE9** | IN1 | U 相 PWM (TIM1_CH1) |
| **PE11** | IN2 | V 相 PWM (TIM1_CH2) |
| **PE13** | IN3 | W 相 PWM (TIM1_CH3) |
| **PE14** | EN | 驱动使能 |
| **PC4** | NSP (nSLEEP) | ⚠️ 必须拉高，否则芯片休眠 |
| GND | GND | 共地 |

### SimpleFOC Mini → 电机（三相直连）

```
M1 ──────────────────────────── 电机 黑色线
M2 ──────────────────────────── 电机 红色线
M3 ──────────────────────────── 电机 白色线
```

当前没有 INA240，M1/M2/M3 直接连接电机三相。PA3 不接线，默认固件不会初始化 ADC1。

### 可选 INA240 → STM32

只有需要真实电流环时才安装。安装后将 `CURRENT_SENSE_PRESENT` 改为 1。

| INA240 | STM32 |
|--------|-------|
| OUT | **PA3** (ADC1_IN3) |

### AS5600 编码器 → STM32

| AS5600 | STM32 |
|--------|-------|
| VCC | 3.3V |
| GND | GND |
| SCL | **PB6** (I2C1_SCL) |
| SDA | **PB7** (I2C1_SDA) |

### TJA1050 CAN 收发器 → STM32

| TJA1050 | STM32 |
|---------|-------|
| TXD | **PD1** (CAN1_TX) |
| RXD | **PD0** (CAN1_RX) |
| VCC | 5V |
| GND | GND |
| S | **GND** ← ⚠️ 必须接地，悬空=静默模式 |

### ST-Link 调试

| ST-Link | STM32 |
|---------|-------|
| SWDIO | PA13 |
| SWCLK | PA14 |
| GND | GND |

### 电源

- SimpleFOC Mini VM → **12V**
- STM32、AS5600 → **3.3V**
- TJA1050 → **5V**
- **全部共地**

---

## 引脚总表

```
┌────────┬──────────────────────────┐
│ PE9    │ SimpleFOC IN1 (U相PWM)    │
│ PE11   │ SimpleFOC IN2 (V相PWM)    │
│ PE13   │ SimpleFOC IN3 (W相PWM)    │
│ PE14   │ SimpleFOC EN              │
│ PC4    │ SimpleFOC NSP (nSLEEP)    │
│ PA3    │ 可选 INA240 OUT（当前未接） │
│ PB6    │ AS5600 SCL                │
│ PB7    │ AS5600 SDA                │
│ PD0    │ TJA1050 RXD               │
│ PD1    │ TJA1050 TXD               │
│ PA13   │ ST-Link SWDIO             │
│ PA14   │ ST-Link SWCLK             │
└────────┴──────────────────────────┘
```

---

## 技术方案

### 软件架构

```
Application
    ├── CANopen Node (对象字典, PDO, SDO, NMT, 状态机)
    ├── Motion Control (轮廓位置模式, 速度模式, 回零)
    └── Motor Driver (电压型 FOC、速度环/位置环、堵转保护)
Middleware
    ├── CANopenNode (CANopen 协议栈)
    └── FreeRTOS
HAL Drivers
    ├── CAN, I2C, TIM(PWM)；ADC 仅有 INA240 时启用
```

### 核心技术栈

| 模块 | 选型 |
|------|------|
| CANopen 协议栈 | CANopenNode (Apache 2.0, 活跃维护, ESA 评审) |
| RTOS | FreeRTOS (ARM_CM3 端口, 避免 FPU 问题) |
| FOC | 自研 (Clarke/Park/SVPWM, 参考 SimpleFOC) |
| 电流采样 | 默认无；INA240A1 为可选升级 |
| 位置反馈 | AS5600 I2C 磁编码器 (12 位, 4096 CPR) |

### CiA 402 对象字典核心条目

| 索引 | 名称 | 类型 |
|------|------|------|
| 6040h | Controlword | U16 |
| 6041h | Statusword | U16 |
| 6060h | Modes of operation | I8 |
| 6061h | Modes of operation display | I8 |
| 6064h | Position actual value | I32 |
| 606Ch | Velocity actual value | I32 |
| 607Ah | Target position | I32 |
| 60FFh | Target velocity | I32 |
| 6081h | Profile velocity | U32 |
| 6083h | Profile acceleration | U32 |
| 6084h | Profile deceleration | U32 |

### CANopen 通讯参数

- 节点 ID: 0x01
- 波特率: 1 Mbps
- TPDO1: Statusword + Position + Velocity (异步, 1-2ms)
- RPDO1: Controlword + Target (SYNC 触发)
- Heartbeat: 1s
- SDO Server: 1 通道

### FOC 参数

- PWM 频率: 20 kHz (TIM1 中心对齐)
- 电压型 FOC: 20 kHz (TIM1 更新中断)
- 速度/位置环: 1 kHz (TIM7)
- 电流采样: 默认无；安装 INA240 后可启用

---

## 参考资源

- CANopenNode: https://github.com/CANopenNode/CANopenNode
- CanOpenSTM32: https://github.com/CANopenNode/CanOpenSTM32
- FreeRTOS-Kernel: https://github.com/FreeRTOS/FreeRTOS-Kernel
- 参考 FOC 项目: `C:\Users\13957\Documents\PlatformIO\Projects\foc`
- FOC 资料: `C:\Users\13957\Desktop\foc资料`
- USB-CAN 工具: `C:\Users\13957\Desktop\固件源码工具`
